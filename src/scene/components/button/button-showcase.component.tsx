import {Button, ButtonProps} from '@ui-kitten/components';
import React from 'react';

const ButtonShowcase = (props?: ButtonProps) => {
  return <Button {...props}>BUTTON</Button>;
};

export default ButtonShowcase;
